prefix=/usr/local
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: libmaus2digests
Description: non pic digest routines in libmaus2
Requires: zlib 
Version: 2.0.76
Libs: -L${libdir} -lmaus2digests -lmaus2digests_sse4
Cflags:   -fopenmp  -std=gnu++0x    -I${includedir}
